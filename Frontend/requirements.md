## Packages
recharts | For visualizing load test metrics and performance data
framer-motion | For smooth page transitions and micro-interactions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Authentication is handled via Replit Auth (use-auth.ts).
Razorpay integration expects `window.Razorpay` to be available.
Load test simulation will be visualized on the frontend.
